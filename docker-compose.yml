services:
  devpulse:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["bun", "run", "start"]
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - devpulse-data:/app/data

volumes:
  devpulse-data:
